{
  "build": {
    "command": "composer install && php artisan config:cache"
  },
  "start": {
    "command": "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=8080",
    "port": 8080
  }
}
